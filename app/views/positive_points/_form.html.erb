<%= form_for(@positive_point) do |f| %>
  <% if @positive_point.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@positive_point.errors.count, "error") %> prohibited this positive_point from being saved:</h2>

      <ul>
      <% @positive_point.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
